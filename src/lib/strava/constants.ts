/** Name of the short-lived httpOnly cookie used to carry the OAuth `state`
 * value between /api/strava/authorize and /api/strava/callback. */
export const STRAVA_OAUTH_STATE_COOKIE = "strava_oauth_state";
